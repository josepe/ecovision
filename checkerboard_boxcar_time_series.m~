function y=checkerboard_boxcar_time_series(sampling_frequency,fps,frames_on)
% y=time series vector
% generates boxcar function mirroring a black and white checkerboard contrast inversion movie 
% beggining filename
% num_squares= number of squares
% pixels_per_square=number of pixels per square
% polarity =0 if upper left pixel is white =1 if black




for i=1:20
    for j=0:24
        
 
    for j=25:49
        file_index=num2str(50*(i-1)+j+comienzo+1000000);
        imwrite(check_off./1.5,[ file_index(2:end) '.png'],'png');
     
    end
end
